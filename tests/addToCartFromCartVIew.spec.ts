import { test } from '../fixtures/fixtures.js';

test.describe('Add to cart from quick view', async function () {
  test('Adding to cart from product quick view - FinalProject02', async function ({
    headerElements,
    quickViewBlock,
    searchResultsPage,
  }) {
    await headerElements.searchByText;
  });
});
